{
  "name": "mvpapp-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:smoke": "playwright install --with-deps chromium && playwright test tests/e2e/exploratory-smoke.spec.ts",
    "test:e2e:r1-006": "playwright install --with-deps chromium && playwright test tests/e2e/r1-006-profile-switch.spec.ts",
    "setup:e2e:deps": "playwright install --with-deps chromium",
    "test:e2e:monkeyish": "playwright install --with-deps chromium && playwright test tests/e2e/monkeyish.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}

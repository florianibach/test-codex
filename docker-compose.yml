services:
  app:
    build: .
    ports:
      - "${HOST_PORT:-8080}:8080"
    environment:
      PORT: 8080
      DB_PATH: /app/data/app.db
    volumes:
      - app-data:/app/data

volumes:
  app-data:
